import fs from"node:fs";import{createHash}from"node:crypto";import{createClient}from"genlayer-js";import{studionet}from"genlayer-js/chains";const address=process.env.CONTRACT_ADDRESS as `0x${string}`;if(!address)throw Error("Set CONTRACT_ADDRESS");const client=createClient({chain:studionet}),[code,schema]=await Promise.all([client.getContractCode(address),client.getContractSchema(address)]),local=fs.readFileSync("contracts/AgentIntentBridge.py","utf8"),sha=(v:string)=>createHash("sha256").update(v.replace(/\r\n/g,"\n")).digest("hex");if(!code||sha(code)!==sha(local))throw Error("source mismatch");console.log(JSON.stringify({address,sourceSha256:sha(local),methods:Object.keys((schema as any).methods??{})},null,2));
