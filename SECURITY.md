# Security model

Public pages are untrusted inputs. Each validator independently re-fetches both sources and binds HTTP state, source state, fingerprints, every alignment dimension, hidden-assumption risk, decision and vector fingerprint. Authentication prevents arbitrary parties from attributing interpretations to agents. Reservation prevents pending revision collisions. Activation is controller-only and requires a validator-verified record; revocation permanently closes the scope.
