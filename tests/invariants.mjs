import fs from"node:fs";import assert from"node:assert/strict";const s=fs.readFileSync("contracts/AgentIntentBridge.py","utf8");assert.match(s,/only registered agent/);assert.match(s,/revision_reserved\[key\]=True/);assert.match(s,/leader==validator/);assert.match(s,/if "BROKEN" in verdicts or hidden=="HIGH": return "MISALIGNED"/);assert.match(s,/scope already active/);assert.doesNotMatch(s,/py-genlayer:(test|latest)/);console.log("AgentIntentBridge invariant checks passed: 6\/6");
